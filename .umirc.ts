import { defineConfig } from 'umi';

export default defineConfig({
  locale: {},
  nodeModulesTransform: {
    type: 'none',
  },
  fastRefresh: {},
  theme: {
    'primary-color': '#00cacb',
  },
});
